#!/usr/bin/env bash

function limbo_docker {
  local shome="$(cd -P -- "$(dirname -- "$BASH_SOURCE")/.." && pwd -P)"
  source "$shome/script/profile"

  export DOCKER_HOST="tcp://${BASEBOX_IP}:2375"
  
  exec docker "$@"
}

limbo_docker "$@"
