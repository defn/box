#!/usr/bin/env bash

function bootstrap {
  local shome="$(cd -P -- "$(dirname -- "$BASH_SOURCE")/.." && pwd -P)"
  source "$shome/script/profile"

  pkg_ensure misc/getopt
  pkg_ensure net/wget
  pkg_ensure www/curl
}

bootstrap
