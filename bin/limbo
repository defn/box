#!/usr/bin/env bash

function main {
  local shome="${_limbo_home:-"$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"}"
  source "$shome/script/profile"
  source limbo_common

  box_main "$@"
}

export SUB_MIDDLEWARE='limbo'
source sub "$BASH_SOURCE" "$@"
